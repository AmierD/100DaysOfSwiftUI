import Cocoa

let array = ["Google", "Apple", "Microsoft", "Google"]
let set = Set(array)

print("Number of items in the array: \(array.count)")
print("Number of unique items in the array: \(set.count)")
